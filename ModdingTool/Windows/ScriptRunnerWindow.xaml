<Controls:MetroWindow x:Class="ModdingTool.Windows.ScriptRunnerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        mc:Ignorable="d"
        Title="Script Runner" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        BorderThickness="0"
        WindowTitleBrush="{DynamicResource AppTitleBarGradient}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="SaveScriptButton" Content="Save Script" Width="100" Height="28" Margin="0,0,8,0" Click="SaveScriptButton_Click"/>
                <Button x:Name="LoadScriptButton" Content="Load Script" Width="100" Height="28" Click="LoadScriptButton_Click"/>
            </StackPanel>
            <TextBlock Text="Python Script:" FontWeight="Bold" Margin="0,8,0,4"/>
        </StackPanel>
        <avalonedit:TextEditor x:Name="ScriptEditor" Grid.Row="1" FontFamily="Consolas" FontSize="14" ShowLineNumbers="True" SyntaxHighlighting="Python" Margin="0,0,0,8"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,8">
            <Button x:Name="RunButton" Content="Run Script" Width="120" Height="32" Margin="0,0,8,0" Click="RunButton_Click"/>
            <Button x:Name="ForceStopButton" Content="Force Stop" Width="120" Height="32" IsEnabled="False" Click="ForceStopButton_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="3">
            <TextBlock Text="Results:" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBox x:Name="ResultsTextBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" IsReadOnly="True" Height="120"/>
        </StackPanel>
    </Grid>
</Controls:MetroWindow> 