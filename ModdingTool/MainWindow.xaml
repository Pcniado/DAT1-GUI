<Controls:MetroWindow x:Class="ModdingTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ModdingTool"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="DAT1 GUI" Height="550" Width="950" Closing="Window_Closing"
        WindowTitleBrush="{StaticResource AppTitleBarGradient}"
        BorderThickness="0"
        GlowBrush="{x:Null}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid Background="#232323">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="71*"/>
            <ColumnDefinition Width="729*"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="Overlay" Panel.ZIndex="1000" Visibility="Collapsed" Grid.ColumnSpan="2">
            <Grid.Background>
                <SolidColorBrush Color="#BF232323"/>
            </Grid.Background>

            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="80" Background="#CC574EB9">
                <TextBlock x:Name="OverlayHeaderLabel" Text="Installing..." HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="18" Margin="25,15,0,0" Foreground="White" />
                <TextBlock x:Name="OverlayOperationLabel" Text="1/1) Patching TOC..." HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="14" Margin="25,45,0,0" Foreground="White" />
            </Grid>
        </Grid>
        <DockPanel Grid.ColumnSpan="2">
            <StackPanel DockPanel.Dock="Top">
                <Menu Padding="0,0,0,0" BorderThickness="0,0,0,2" BorderBrush="#232323" Background="#232323" Foreground="#eee">
                    <MenuItem Header="File" SubmenuOpened="File_SubmenuOpened">
                        <MenuItem x:Name="File_LoadToc" Header="Load 'toc'..." Click="File_LoadToc_Click" />
                        <MenuItem x:Name="File_LoadRecent" Header="Load Recent">
                            <MenuItem x:Name="File_LoadRecent1" Click="File_LoadRecentItem_Click" />
                            <MenuItem x:Name="File_LoadRecent2" Click="File_LoadRecentItem_Click" />
                            <MenuItem x:Name="File_LoadRecent3" Click="File_LoadRecentItem_Click" />
                            <MenuItem x:Name="File_LoadRecent4" Click="File_LoadRecentItem_Click" />
                            <MenuItem x:Name="File_LoadRecent5" Click="File_LoadRecentItem_Click" />
                        </MenuItem>
                    </MenuItem>
                    <MenuItem Header="Project">
                        <MenuItem Header="New Project" Click="NewProject_Click"/>
                        <MenuItem Header="Open Project" Click="OpenProject_Click"/>
                        <MenuItem Header="Open Recent Project" x:Name="OpenRecentProjectMenu">
                            <MenuItem Header="(No recent projects)" IsEnabled="False" x:Name="NoRecentProjectsItem"/>
                        </MenuItem>
                        <MenuItem Header="Project Settings" Click="ProjectSettings_Click" x:Name="ProjectSettingsMenuItem"/>
                    </MenuItem>
                    <MenuItem Header="Search">
                        <MenuItem x:Name="Search_Search" Header="Search..." Click="Search_Search_Click" />
                        <MenuItem x:Name="Search_JumpTo" Header="Jump to path or ref..." Click="Search_JumpTo_Click" />
                    </MenuItem>
                    <MenuItem Header="Mod" SubmenuOpened="Mod_SubmenuOpened">
                        <MenuItem x:Name="Mod_ReplacedItemsCount" Header="0 replaced, 0 new" IsEnabled="false" />
                        <MenuItem x:Name="Mod_ClearReplaced" Header="Clear all" Click="Mod_ClearReplaced_Click" />
                        <Separator/>
                        <MenuItem x:Name="Mod_ReplaceAssetsFromStage" Header="Add from stage..." Click="Mod_ReplaceAssetsFromStage_Click" />
                        <MenuItem x:Name="Mod_CreateFromReplaced" Header="Pack as .stage..." Click="Mod_CreateFromReplaced_Click" />
                        <Separator/>
                        <MenuItem x:Name="Mod_CreateModular" Header="Create .modular..." Click="Mod_CreateModular_Click" />
                    </MenuItem>
                    <MenuItem Header="Tools">
                        <MenuItem x:Name="Tools_CalculateHash" Header="Calculate hash..." Click="Tools_CalculateHash_Click" />
                        <MenuItem x:Name="Tools_ConfigEditor" Header="Config Editor..." Click="Tools_ConfigEditor_Click" />
                        <MenuItem x:Name="Tools_ScriptRunner" Header="Script Runner..." Click="Tools_ScriptRunner_Click" />
                        <MenuItem x:Name="Tools_TextureViewer" Header="Texture Viewer..." Click="Tools_TextureViewer_Click" />
                        <MenuItem x:Name="Tools_WemPlayer" Header="Wem Player..." Click="Tools_WemPlayer_Click" />
                    </MenuItem>
                </Menu>
            </StackPanel>

            <StatusBar DockPanel.Dock="Bottom" BorderBrush="#232323" BorderThickness="0,2,0,0" Background="#232323" Foreground="#eee">
                <StatusBar.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="180" />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </StatusBar.ItemsPanel>
                <StatusBarItem Grid.Column="0">
                    <TextBlock x:Name="CurrentPath" Text="Use &quot;File &gt; Load 'toc'...&quot; to load 'toc'" Padding="2,1,2,2" />
                </StatusBarItem>
                <Separator Grid.Column="1" Background="#CCC" />
                <StatusBarItem Grid.Column="2">
                    <TextBlock x:Name="DirectoryDetails" Padding="2,1,2,2" />
                </StatusBarItem>
            </StatusBar>


            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="250*" MinWidth="100" />
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="550*" MinWidth="100" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TreeView x:Name="Folders" BorderBrush="#444" BorderThickness="0,0,2,0" Padding="0,4,4,4" SelectedItemChanged="Folders_SelectedItemChanged" ContextMenuOpening="Folders_ContextMenuOpening">
                        <TreeView.ContextMenu>
                            <ContextMenu>
                                <MenuItem x:Name="FoldersMenu_ExtractAssets" Header="Extract all..." Click="FoldersMenu_ExtractAssets_Click" />
                                <MenuItem x:Name="FoldersMenu_ExtractAssetsToStage" Header="Extract to stage..." Click="FoldersMenu_ExtractAssetsToStage_Click" />
                                <Separator/>
                                <MenuItem x:Name="FoldersMenu_CopyPath" Header="Copy path" Click="FoldersMenu_CopyPath_Click" />
                            </ContextMenu>
                        </TreeView.ContextMenu>
                    </TreeView>
                </Grid>
                <GridSplitter Background="Transparent" Grid.Column="1" Margin="0" Width="3" VerticalAlignment="Stretch" HorizontalAlignment="Center" />
                <Grid Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <DataGrid x:Name="AssetsList"
                              RowHeaderWidth="0"
                              GridLinesVisibility="Horizontal"
                              Background="#232323"
                              Foreground="#eee"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              BorderThickness="0"
                              SelectionMode="Extended"
                              ContextMenuOpening="AssetsList_ContextMenuOpening"
                              PreviewMouseRightButtonDown="AssetsList_PreviewMouseRightButtonDown"
                              HorizontalScrollBarVisibility="Hidden">
                        <DataGrid.Resources>
                            <SolidColorBrush x:Key="DataGridRowBackground" Color="#232323"/>
                            <SolidColorBrush x:Key="DataGridAlternatingRowBackground" Color="#31313a"/>
                            <SolidColorBrush x:Key="DataGridRowForeground" Color="#eee"/>
                            <SolidColorBrush x:Key="DataGridCellForeground" Color="#eee"/>
                            <SolidColorBrush x:Key="DataGridColumnHeaderBackground" Color="#232323"/>
                            <SolidColorBrush x:Key="DataGridColumnHeaderForeground" Color="#eee"/>
                            <SolidColorBrush x:Key="DataGridGridLinesBrush" Color="#444"/>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="File Name" Width="*" Binding="{Binding Name}" IsReadOnly="True" />
                            <DataGridTextColumn Header="Size" Width="80" Binding="{Binding SizeFormatted}" SortMemberPath="Size" IsReadOnly="True" />
                            <DataGridTextColumn Header="Span" Width="55" Binding="{Binding Span}" IsReadOnly="True" />
                            <DataGridTextColumn Header="Archive" Width="100" Binding="{Binding Archive}" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </DockPanel>
    </Grid>
</Controls:MetroWindow>
